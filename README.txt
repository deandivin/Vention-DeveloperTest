To run, open file 'main' with a web browser. (Was tested with Google Chrome)
    -Right click main.html
    -Scroll to 'Open with'
    -Select Web browser

Assumptions:
    - Cube A spawns on left side, Cube B spawns on right side
    - Cube B's dynamics remain the same post collision if not specified in test document.